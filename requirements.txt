# Pinned to what stage 3 was run and reported with.
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt

# stages 1-2: captioning and embedding through the OpenAI-compatible gateway
openai==3.3.1
pillow==12.3.0

# stages 1-3
numpy==2.5.2

# stage 3: training, metrics, tests
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.1
pyarrow==25.0.1
pytest==9.1.1
